export enum ThreadType {
    User,
    Group,
}

export enum DestType {
    User = 3,
    Page = 5,
}

export enum ReminderRepeatMode {
    None = 0,
    Daily = 1,
    Weekly = 2,
    Monthly = 3,
}

export enum Gender {
    Male = 0,
    Female = 1,
}

export enum BoardType {
    Note = 1,
    PinnedMessage = 2,
    Poll = 3,
}
