export enum ThreadType {
    User,
    Group,
}

export enum DestType {
    Group = 1,
    User = 3,
    Page = 5,
}

export enum Gender {
    Male = 0,
    Female = 1,
}

export enum AvatarSize {
    Small = 120,
    Large = 240,
}
